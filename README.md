# Image Processing and Vector Storage System

A system for processing images using CoreML and storing vectors in ChromaDB.

## Setup

1. Create a virtual environment:
```bash
python -m venv .venv
source .venv/bin/activate